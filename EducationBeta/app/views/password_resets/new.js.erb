var $modal = $('#modal'),
    $modal_close = $modal.find('.close'),
    $modal_container = $('#modal-container');

$modal.html('<%=escape_javascript render :partial => "password_reset" %>').prepend($modal_close).css('top', $(window).scrollTop() + 80).css('width', '400px').show();
$modal_container.show();
