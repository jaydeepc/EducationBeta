var $modal = $('#modal'),
    $modal_close = $modal.find('.close'),
    $modal_container = $('#modal-container');

$modal.html('<%=escape_javascript render :partial => "send_validation_token" %>').prepend($modal_close).css('top', $(window).scrollTop() + 80).show();
$modal_container.show();
